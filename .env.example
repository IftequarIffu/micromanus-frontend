# Same Supabase project as the micromanus backend
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Leave unset in local dev so requests use the Vite proxy (same-origin).
# Required on Vercel: absolute backend origin, no trailing slash.
# VITE_API_URL=https://<backend>.vercel.app
